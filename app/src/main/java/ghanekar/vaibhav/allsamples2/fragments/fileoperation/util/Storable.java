package ghanekar.vaibhav.allsamples2.fragments.fileoperation.util;

/**
 * As it sounds, anything that can stored and represented as byte array.
 */
public interface Storable {
    byte[] getBytes();
}
